<?xml version="1.0" encoding="utf-8"?>
<!-- The `overview_root` id is the UiAutomator anchor used by OverviewTest. The root class is a
     Compose-hosting FrameLayout subclass; content is a full-screen scrim + head + LazyRow of
     recent-task cards rendered by `OverviewLayout`. -->
<com.boringdroid.systemui.overview.OverviewLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overview_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
