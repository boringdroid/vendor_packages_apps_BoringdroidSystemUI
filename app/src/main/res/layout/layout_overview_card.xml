<?xml version="1.0" encoding="utf-8"?>
<!-- Single overview card. Horizontally laid out in layout_overview.xml's
     RecyclerView. Thumbnail is bound from ActivityManagerWrapper.getTaskThumbnail. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#22FFFFFF"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:orientation="vertical"
    android:padding="12dp">

    <ImageView
        android:id="@+id/overview_card_thumbnail"
        android:layout_width="240dp"
        android:layout_height="160dp"
        android:background="#44000000"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/overview_card_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/overview_card_label"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:gravity="center_horizontal"
        android:maxLines="1"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp" />
</LinearLayout>
